# Shared secret used by PlatformPilot to authenticate ingestion requests.
# Generate a long random value for local, staging, and production environments.
# Never commit the real value.
PLATFORM_PILOT_INGEST_TOKEN=replace-with-a-long-random-token